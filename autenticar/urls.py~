# -*- coding: utf-8 -*-
from django.conf.urls import patterns, include, url

urlpatterns = patterns('autenticar.views',
    url(r'^$', 'index'),
    url(r'^/select_entidad/$', 'select_entidad'),
    #url(r'^(?P<poll_id>\d+)/$', 'detail'),
    #url(r'^$', 'index'),
    #url(r'^(?P<poll_id>\d+)/$', 'detail'),
    #url(r'^(?P<poll_id>\d+)/results/$', 'results'),
    #url(r'^(?P<poll_id>\d+)/vote/$', 'vote'),
    #url(r'^static/(?P.*)$', 'django.views.static.serve',{'document_root': '/home/juanjo/django/misitio/static_files'}),
)
